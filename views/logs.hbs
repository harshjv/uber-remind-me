<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Uber Remind Me!</title>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <div class="container">
      <div class="page-header">
        <h1>Logs <small><code>{{logLength}}</code></small></h1>
      </div>
      <div class="table-responsive">
        <table class="table table-striped">
          <thead>
            <tr>
              <td>Email</td>
              <td>Google Maps API Requests <small><code>{{googleMapsAPICount}}</code></small></td>
              <td>Uber API Requests <small><code>{{uberAPICount}}</code></small></td>
              <td>Scheduled X times <small><code>{{scheduleCount}}</code></small></td>
              <td>Scheduled on</td>
            </tr>
          </thead>
          <tbody>
            {{#each logs}}
              <tr>
                <td>{{@key}}</td>
                <td>{{this.googleMapsAPICount}}</td>
                <td>{{this.uberAPICount}}</td>
                <td>{{this.scheduleCount}}</td>
                <td>{{this.scheduledOn}}</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
